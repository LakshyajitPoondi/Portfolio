# Server-only values. Never put these in browser JavaScript or commit .env.local.
RESEND_API_KEY=
# Sender address on a domain verified in Resend:
CONTACT_FROM=
# Photographer's destination inbox:
CONTACT_TO=
# Optional exact origin when using a custom domain, e.g. https://photos.example.com
CONTACT_ORIGIN=
